import React from 'react';

interface MemberRecordProps {}

const MemberRecord = (props: MemberRecordProps): JSX.Element=> {
    return <h1>This is Record Page, display if Member wants to see reserve Record</h1>
}

export default MemberRecord;